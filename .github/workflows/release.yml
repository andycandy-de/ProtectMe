 - name: Release
      uses: qcastel/github-actions-maven-release@v1.11.1
      with:
        release-branch-name: "master"

        git-release-bot-name: "release-bot"
        git-release-bot-email: "release-bot@andycandy.de"

        access-token: ${{ secrets.GITHUB_ACCESS_TOKEN }}
